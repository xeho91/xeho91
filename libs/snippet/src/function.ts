/**
 * Snippets related to using {@link Array}.
 * @module
 */

/**
 * **No operation**.
 * performs no action but can be invoked like any other function.
 * It can used as a placeholder or default behavior.
 * @returns
 */
export const noop = () => ({});
